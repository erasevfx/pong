import pygame
from os.path import join

WINDOW_WIDTH, WINDOW_HEIGHT = 2560, 1440
SIZE = {'paddle': (80,200), 'ball': (60,60)}
POS = {'player': (WINDOW_WIDTH - 100, WINDOW_HEIGHT / 2), 'opponent': (100, WINDOW_HEIGHT / 2)}